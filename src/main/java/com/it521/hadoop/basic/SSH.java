package com.it521.hadoop.basic;

/**
 * @author Ming
 * @描述 SSH 免密登录
 *
 * scp -r /usr/local/apps/jdk b3data-02:/usr/local/apps/
 */
public class SSH {

    /**
     * SSH 远程登录的安全外壳协议
     *
     * 有两种身份验证机制
     */

    /**
     * 实际操作   @2 ssh-keygen - t rsa
     */
    /*@1 cd ~
     *
     *@2 ssh-keygen
     *
     *@3 ssh-copy-id 主机名或IP映射
     */
}
